# Runtime + dev dependencies for MaterialSelect AI backend.
# Prefer `pip install -e ".[dev]"` (reads pyproject.toml); this file is a
# convenience pin list for environments without editable installs.
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
pint>=0.24
python-dotenv>=1.0

# Dev / test
pytest>=8.2
httpx>=0.27
ruff>=0.5
black>=24.4
